name: First Interaction

on:
  pull_request:
    types: [opened]
  issues:
    types: [opened]

jobs:
  welcome:
    name: Welcome
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        pr-message: "Welcome 👋🏻! Thank you for your first contribution :)"
        issue-message: "Welcome 👋🏻! Thank you for your first contribution :)"
